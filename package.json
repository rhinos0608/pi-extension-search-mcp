{
  "name": "pi-atlas",
  "version": "0.1.0",
  "description": "Pi extension — web search, fetch, GitHub, social, media, browser, and desktop tools with multi-provider search, academic research, and agent-browser automation.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "pi-extension-search": "./bin/pi-extension-search.mjs"
  },
  "files": [
    "bin/",
    "src/*.ts",
    "README.md",
    "SKILL.md"
  ],
  "scripts": {
    "cli": "node --import tsx src/cli.ts",
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.79.10",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agent-browser": "0.32.0",
    "tsx": "^4.21.0",
    "typebox": "^1.3.2"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.10",
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
